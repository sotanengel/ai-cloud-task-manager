# Takumi Guard (Shisho Cloud supply chain security)
# Anonymous mode: レジストリをプロキシ経由に変更するだけで有効
# 有料プランでトークン認証が必要な場合: //registry.shisho.dev/:_authToken=${TAKUMI_GUARD_TOKEN}
#
# 利用する場合は以下のコメントを外す:
# registry=https://registry.shisho.dev/npm/
#
# 現在は npm 公式レジストリを使用:
registry=https://registry.npmjs.org/
